let userName: string = "Alex";

userName = "keka"

const arr: number[] = []

arr.push(1)

type UserType = {
    name: string;
    age: string;
}

const user: UserType = {
    name: "keka",
    age: "26",
}